- name: Install packages
  apt:
    name:
      - nfs-common
      - unzip
      - jq
      - pacemaker
      - corosync
      - pcs
      - gettext-base
      - python3-pip
    state: present

- pip:
    name: awscli
